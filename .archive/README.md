# Archive

This folder contains archived development documentation and legacy code.

## Contents

- `docs/` - Development documentation (API fixes, kanban board, etc.)
- `legacy-shared/` - Old shared types/utils (replaced by `packages/shared/`)

These files are kept for historical reference but are not part of the active codebase.
